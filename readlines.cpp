#include <iostream>
#include "stabbingLines.h"
#include <fstream>
using namespace std;
void readLines(ifstream& inPutLineFile, Line linesArray[],
const int MaxLnsSize, int& numLines){
if (inPutPointFile.is_open()){
        int i=0;

    while(! inPutLineFile.eof()){
            inPutLineFile >> pointsArray[i].Pid >>pointsArray[i].p1 >> pointsArray[i].p2
            i++;
}
}
inPutLineFile.close();
}

























}
